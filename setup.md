# Инструкция по настройке Hotel Content Automation

Пошаговое руководство по настройке системы автоматизации контента для отеля Atlas Apart.

## 📋 Предварительные требования

1. Python 3.10 или выше
2. Git
3. FFmpeg
4. Аккаунт Google Cloud Platform
5. Аккаунт OpenAI
6. Telegram аккаунт

## 🔧 Шаг 1: Установка зависимостей

### Python и виртуальное окружение

```bash
# Клонируйте репозиторий
git clone https://github.com/alenakonovalova30055/hotel-content-automation.git
cd hotel-content-automation

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
# Linux/Mac:
source venv/bin/activate
# Windows:
venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt
```

### FFmpeg

#### Linux (Ubuntu/Debian)
```bash
sudo apt update
sudo apt install ffmpeg
```

#### macOS
```bash
brew install ffmpeg
```

#### Windows
1. Скачайте FFmpeg: https://ffmpeg.org/download.html
2. Распакуйте архив
3. Добавьте папку `bin` в PATH

Проверка установки:
```bash
ffmpeg -version
```

## ☁️ Шаг 2: Настройка Google Cloud

### 2.1 Создание проекта

1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/)
2. Создайте новый проект: "Hotel Content Automation"
3. Запишите Project ID

### 2.2 Включение API

Перейдите в "APIs & Services" → "Library" и включите:
- Google Drive API
- Google Docs API

### 2.3 Создание Service Account

1. Перейдите в "IAM & Admin" → "Service Accounts"
2. Нажмите "Create Service Account"
3. Заполните:
   - Name: `hotel-automation`
   - Description: `Service account for hotel content automation`
4. Нажмите "Create and Continue"
5. Grant access: "Editor" или "Owner"
6. Нажмите "Done"

### 2.4 Создание ключа

1. Найдите созданный Service Account в списке
2. Нажмите на него → вкладка "Keys"
3. "Add Key" → "Create new key"
4. Выберите JSON
5. Сохраните файл как `service-account.json` в корне проекта

**⚠️ ВАЖНО:** Не публикуйте этот файл в открытом доступе!

### 2.5 Настройка доступа к Google Drive

1. Откройте файл `service-account.json`
2. Найдите поле `client_email` (например: `hotel-automation@project-id.iam.gserviceaccount.com`)
3. Откройте Google Drive папку: https://drive.google.com/drive/folders/1ahvhQ7r0yU_TIqC-UHdw9dRfBZAELUeV
4. Нажмите "Share" / "Поделиться"
5. Добавьте email из `client_email` с правами "Editor" / "Редактор"
6. Отправьте приглашение

## 🤖 Шаг 3: Настройка OpenAI

1. Перейдите на [OpenAI Platform](https://platform.openai.com/)
2. Войдите или зарегистрируйтесь
3. Перейдите в "API Keys": https://platform.openai.com/api-keys
4. Нажмите "Create new secret key"
5. Скопируйте ключ (он показывается только один раз!)
6. Убедитесь, что на аккаунте есть баланс

**Стоимость:** ~$0.03-0.06 на одну генерацию текста с GPT-4

## 📱 Шаг 4: Настройка Telegram Bot

### 4.1 Создание бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду: `/newbot`
3. Введите имя бота: `Atlas Apart Content Bot`
4. Введите username: `atlas_apart_content_bot` (должен быть уникальным)
5. Скопируйте Token (формат: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 4.2 Получение User ID

**Способ 1: Через бота**
1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Бот покажет ваш User ID

**Способ 2: Через другого бота**
1. Найдите [@raw_data_bot](https://t.me/raw_data_bot)
2. Отправьте любое сообщение
3. Найдите `"id"` в ответе

**Для Atlas Apart:** User ID уже известен: `220317440`

### 4.3 Запуск бота (для тестирования)

После настройки .env можно запустить бота:
```bash
python -c "from src.telegram_bot import TelegramBot; bot = TelegramBot('YOUR_TOKEN', 220317440); bot.run_polling()"
```

Отправьте боту команду `/start` чтобы убедиться, что он работает.

## ⚙️ Шаг 5: Конфигурация .env

1. Скопируйте пример конфигурации:
```bash
cp .env.example .env
```

2. Откройте `.env` в текстовом редакторе:
```bash
nano .env
# или
code .env
# или любой другой редактор
```

3. Заполните параметры:

```env
# OpenAI API Key (из шага 3)
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Google Drive (из шага 2)
GOOGLE_DRIVE_FOLDER_ID=1ahvhQ7r0yU_TIqC-UHdw9dRfBZAELUeV
GOOGLE_SERVICE_ACCOUNT_FILE=service-account.json

# Telegram Bot (из шага 4)
TELEGRAM_BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_USER_ID=220317440

# Структура папок (обычно не нужно менять)
RAW_VIDEO_FOLDER=RAW_VIDEO
RAW_PHOTO_FOLDER=RAW_PHOTO
TEXTS_FOLDER=TEXTS
POSTS_REFERENCE_FOLDER=POSTS_REFERENCE
READY_REELS_FOLDER=READY_REELS
PROCESSED_FOLDER=PROCESSED

# Настройки контента (можно настроить)
VIDEO_PERCENTAGE=70
CAROUSEL_PERCENTAGE=30
MIN_VIDEO_DURATION=10
MAX_VIDEO_DURATION=30

# Стили текста
TEXT_COLORS=white,#FFE5B4,#FFB6C1,#ADD8E6
FONT_PATH=templates/fonts/
```

4. Сохраните файл

## 📁 Шаг 6: Проверка структуры Google Drive

Убедитесь, что в Google Drive создана правильная структура:

```
CONTENT_ATLAS/
├── RAW_VIDEO/          ← Загрузите сюда видео (MP4, MOV)
├── RAW_PHOTO/          ← Загрузите сюда фото (JPG, PNG)
├── TEXTS/              ← Создайте текстовый файл с описанием отеля
├── POSTS_REFERENCE/    ← Добавьте 10-20 примеров постов
├── READY_REELS/        ← Готовые видео (создается автоматически)
└── PROCESSED/          ← Архив (создается автоматически)
```

### Рекомендации:
- **RAW_VIDEO**: Видео 10-30 секунд, формат MP4 или MOV
- **RAW_PHOTO**: Качественные фото в высоком разрешении (JPG, PNG)
- **TEXTS**: Текстовый файл с описанием отеля (~200-500 слов)
- **POSTS_REFERENCE**: Текстовые файлы с примерами постов (по одному посту в файле)

## ✅ Шаг 7: Проверка установки

### Проверка Python модулей
```bash
python -c "from src.google_drive import GoogleDriveHandler; print('✅ Google Drive OK')"
python -c "from src.openai_handler import OpenAIHandler; print('✅ OpenAI OK')"
python -c "from src.video_editor import VideoEditor; print('✅ Video Editor OK')"
python -c "from src.telegram_bot import TelegramBot; print('✅ Telegram OK')"
```

### Проверка FFmpeg
```bash
ffmpeg -version
```

### Проверка доступа к Google Drive
```bash
python -c "
from src.google_drive import GoogleDriveHandler
import os
from dotenv import load_dotenv
load_dotenv()
handler = GoogleDriveHandler(
    os.getenv('GOOGLE_SERVICE_ACCOUNT_FILE'),
    os.getenv('GOOGLE_DRIVE_FOLDER_ID')
)
videos = handler.get_video_files()
print(f'✅ Found {len(videos)} videos in Google Drive')
"
```

## 🚀 Шаг 8: Первый запуск

```bash
python main.py
```

Система выполнит:
1. ✅ Подключение к Google Drive
2. ✅ Загрузку материалов (видео или фото)
3. ✅ Загрузку описания отеля и примеров постов
4. ✅ Генерацию текста через OpenAI
5. ✅ Монтаж видео с текстом
6. ✅ Отправку видео в Telegram

### Проверка результатов

1. **В консоли:** Вы увидите логи всех операций
2. **В папке logs/:** Создастся файл лога с timestamp
3. **В папке output/:** Сохранится готовое видео
4. **В Telegram:** Придет видео с кнопками одобрения/отклонения

## 🔍 Устранение проблем

### "Missing required environment variables"
- Проверьте, что `.env` файл существует и заполнен
- Убедитесь, что все обязательные переменные установлены

### "Failed to authenticate with Google Drive"
- Проверьте путь к `service-account.json`
- Убедитесь, что Service Account добавлен в Google Drive папку
- Проверьте, что Google Drive API включен в проекте

### "Folder not found in Google Drive"
- Проверьте имена папок в Google Drive
- Убедитесь, что папки находятся в корневой папке `CONTENT_ATLAS`
- Проверьте, что Service Account имеет доступ к папкам

### "OpenAI API error"
- Проверьте правильность API ключа
- Убедитесь, что на аккаунте OpenAI есть баланс
- Проверьте интернет-соединение

### "Error sending video to Telegram"
- Проверьте правильность Bot Token
- Проверьте User ID
- Убедитесь, что вы начали диалог с ботом (отправили `/start`)

### "ffmpeg not found"
- Установите FFmpeg как описано в Шаге 1
- Проверьте, что FFmpeg добавлен в PATH

## 📊 Мониторинг

### Просмотр логов в реальном времени
```bash
tail -f logs/process_*.log
```

### Просмотр последнего лог-файла
```bash
ls -t logs/ | head -1 | xargs -I {} cat logs/{}
```

## 🔄 Автоматизация

### Запуск по расписанию (Linux/Mac)

Добавьте в crontab:
```bash
crontab -e
```

Добавьте строку (запуск каждый день в 10:00):
```
0 10 * * * cd /path/to/hotel-content-automation && /path/to/venv/bin/python main.py
```

### Запуск по расписанию (Windows)

Используйте Task Scheduler:
1. Откройте Task Scheduler
2. Create Basic Task
3. Trigger: Daily at 10:00 AM
4. Action: Start a program
5. Program: `C:\path\to\venv\Scripts\python.exe`
6. Arguments: `main.py`
7. Start in: `C:\path\to\hotel-content-automation`

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи в `logs/`
2. Убедитесь, что все шаги выполнены
3. Проверьте права доступа к файлам и папкам
4. Перечитайте инструкцию внимательно

## ✨ Готово!

Теперь система готова к работе. Каждый запуск `main.py` будет:
- Создавать новое видео
- Генерировать уникальный текст
- Отправлять на одобрение в Telegram

Удачи в автоматизации контента! 🚀
