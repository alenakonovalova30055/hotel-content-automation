# Hotel Content Automation

Полная система автоматизации для монтажа видео контента отеля "Atlas Apart" с автоматической генерацией подписей через OpenAI и отправкой готовых видео в Telegram для одобрения перед публикацией в Instagram.

## 🎯 Возможности

- 🎬 **Автоматический монтаж видео** с красивыми текстовыми наложениями
- 🤖 **Генерация текстов через OpenAI** в стиле существующих постов
- 📸 **Создание видео из фото** (карусели) 
- 📱 **Отправка в Telegram** для одобрения перед публикацией
- ☁️ **Интеграция с Google Drive** для хранения материалов
- 📊 **Полное логирование** всех операций

## 📋 Требования

- Python 3.10 или выше
- Google Service Account с доступом к Drive API
- OpenAI API ключ
- Telegram Bot Token
- FFmpeg (для обработки видео)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Клонируйте репозиторий
git clone https://github.com/alenakonovalova30055/hotel-content-automation.git
cd hotel-content-automation

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt
```

### 2. Установка FFmpeg

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install ffmpeg
```

**macOS:**
```bash
brew install ffmpeg
```

**Windows:**
Скачайте FFmpeg с [официального сайта](https://ffmpeg.org/download.html) и добавьте в PATH.

### 3. Настройка конфигурации

Скопируйте `.env.example` в `.env` и заполните все параметры:

```bash
cp .env.example .env
```

Подробная инструкция по настройке в [setup.md](setup.md).

### 4. Запуск

```bash
python main.py
```

## 📁 Структура проекта

```
hotel-content-automation/
├── main.py                      # Главный скрипт
├── requirements.txt             # Зависимости Python
├── .env.example                 # Пример конфигурации
├── .gitignore                   # Игнорируемые файлы
├── README.md                    # Документация
├── setup.md                     # Инструкция по настройке
├── ARCHITECTURE.md              # Описание архитектуры
├── src/                         # Исходный код
│   ├── __init__.py
│   ├── logger.py                # Логирование
│   ├── google_drive.py          # Google Drive API
│   ├── google_docs.py           # Google Docs API
│   ├── openai_handler.py        # OpenAI API
│   ├── text_designer.py         # Дизайн текста
│   ├── video_editor.py          # Монтаж видео
│   └── telegram_bot.py          # Telegram Bot
├── templates/                   # Шаблоны
│   ├── style_guide.json         # Стиль оформления
│   └── fonts/                   # Шрифты
├── logs/                        # Логи
│   └── process_*.log
└── output/                      # Готовые видео
```

## 🔄 Алгоритм работы

1. **Выбор контента**
   - 70% - видео (10-30 сек)
   - 30% - карусели из фото

2. **Генерация текста (OpenAI)**
   - Анализ примеров из POSTS_REFERENCE
   - Использование описания отеля из TEXTS
   - Генерация новой подписи в стиле примеров

3. **Монтаж видео**
   - Добавление текста в случайных позициях
   - Применение плавных переходов (fade, crossfade)
   - Создание видео длительностью 10-30 секунд

4. **Отправка в Telegram**
   - Отправка готового видео файлом
   - Кнопки одобрения/отклонения
   - Текст подписи для Instagram

## 🎨 Стилистика

- **Цвета текста**: белый, пастельные (жёлтый, розовый, голубой)
- **Позиции**: низ, центр, углы (случайно)
- **Шрифт**: Arial (по умолчанию, настраивается)
- **Переходы**: fade, crossfade
- **Длительность**: 10-30 секунд

## 📖 Подробная документация

- [Инструкция по настройке](setup.md) - пошаговая настройка всех компонентов
- [Архитектура системы](ARCHITECTURE.md) - подробное описание модулей

## 🔑 Переменные окружения

| Переменная | Описание |
|------------|----------|
| `OPENAI_API_KEY` | API ключ OpenAI |
| `GOOGLE_DRIVE_FOLDER_ID` | ID папки в Google Drive |
| `GOOGLE_SERVICE_ACCOUNT_FILE` | Путь к JSON файлу Service Account |
| `TELEGRAM_BOT_TOKEN` | Token Telegram бота |
| `TELEGRAM_USER_ID` | ID пользователя Telegram |

Полный список переменных в `.env.example`.

## 🧪 Тестирование

```bash
# Проверка подключения к Google Drive
python -c "from src.google_drive import GoogleDriveHandler; print('OK')"

# Проверка OpenAI
python -c "from src.openai_handler import OpenAIHandler; print('OK')"

# Проверка MoviePy
python -c "from moviepy.editor import VideoFileClip; print('OK')"
```

## 📝 Логи

Все операции логируются в директорию `logs/`:
- Файлы с timestamp: `process_YYYYMMDD_HHMMSS.log`
- Вывод в консоль и файл одновременно
- Уровни: INFO, WARNING, ERROR, DEBUG

## ⚠️ Важные замечания

- Убедитесь, что FFmpeg установлен в системе
- Service Account должен иметь доступ к Google Drive папкам
- Telegram Bot должен быть создан через @BotFather
- OpenAI API требует баланса на аккаунте
- Видео сохраняются в директорию `output/`

## 🐛 Устранение неполадок

**Ошибка: "No module named 'moviepy'"**
```bash
pip install moviepy
```

**Ошибка: "ffmpeg not found"**
Установите FFmpeg как описано в разделе "Установка FFmpeg"

**Ошибка: "Google API authentication failed"**
Проверьте путь к Service Account JSON файлу и права доступа

**Ошибка: "OpenAI API error"**
Проверьте API ключ и баланс на аккаунте OpenAI

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в директории `logs/`
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность настройки `.env`

## 📄 Лицензия

Этот проект создан для автоматизации контента отеля Atlas Apart.

## 🙏 Благодарности

- OpenAI за GPT-4 API
- Google за Drive и Docs API
- MoviePy за библиотеку редактирования видео
- Python Telegram Bot за Telegram интеграцию
